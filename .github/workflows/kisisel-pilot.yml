name: kisisel-pilot

# Kisisellestirme pilotu icin genel kosucu (Mo onayi, 21 Eyl 2026).
# Drive'a erisim yalniz Actions'ta mumkun oldugu icin agir is buraya tasinir:
# secilen daldaki bir script'i rclone yetkili runner'da kosar.
#
# Kapsam siniri: YALNIZ Drive (rclone) kimligi verilir. Etsy ve Prodigi
# secret'lari bu workflow'a hic girmez; Etsy'ye yazan hicbir adim yoktur.
# Tetik yalniz workflow_dispatch'tir (cron yok, push yok).
# Hicbir sir loga yazilmaz.

permissions:
  contents: read

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Script'in okunacagi dal"
        required: true
        default: "kisisel-v1"
      script:
        description: "Kosulacak script yolu (depo koku baz; scripts/ altinda olmali)"
        required: true
        default: "scripts/kisisel/kisisel_pilot.py"
      args:
        description: "Script'e gecilecek ek argumanlar"
        required: false
        default: ""

concurrency: kisisel-pilot

jobs:
  kosu:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout (secilen dal)
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.branch }}

      - name: Python bagimliliklari
        run: |
          set -euo pipefail
          python -m pip install --quiet --upgrade pip
          python -m pip install --quiet pillow opencv-python-headless numpy fonttools

      - name: rclone kur
        run: |
          set -euo pipefail
          curl -fsSL https://rclone.org/install.sh | sudo bash >/dev/null
          rclone version | head -1

      - name: rclone.conf olustur
        env:
          RCLONE_CONF_B64: ${{ secrets.RCLONE_CONF_B64 }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
        run: |
          set -euo pipefail
          if [ -z "${RCLONE_CONF_B64:-}" ]; then
            echo "HATA: RCLONE_CONF_B64 secret'i tanimli degil." >&2
            exit 1
          fi
          mkdir -p ~/.config/rclone
          umask 077
          printf '%s' "$RCLONE_CONF_B64" | base64 -d > ~/.config/rclone/rclone.conf
          chmod 600 ~/.config/rclone/rclone.conf
          # Secret override YALNIZ conf'ta dolu bir client_id varsa yapilir; conf
          # rclone'un varsayilan istemcisini kullaniyorsa override invalid_client verir.
          if grep -qE '^[[:space:]]*client_id[[:space:]]*=[[:space:]]*\S' ~/.config/rclone/rclone.conf; then
            if [ -n "${GOOGLE_CLIENT_SECRET:-}" ]; then
              echo "::add-mask::$GOOGLE_CLIENT_SECRET"
              echo "RCLONE_CONFIG_GDRIVE_CLIENT_SECRET=$GOOGLE_CLIENT_SECRET" >> "$GITHUB_ENV"
            fi
          fi
          echo "rclone.conf yazildi ($(wc -c < ~/.config/rclone/rclone.conf) bayt)."

      - name: Script'i kosu
        env:
          SCRIPT: ${{ inputs.script }}
          ARGS: ${{ inputs.args }}
        run: |
          set -euo pipefail
          case "$SCRIPT" in
            scripts/*) : ;;
            *) echo "HATA: script yolu scripts/ altinda olmali: $SCRIPT" >&2; exit 1 ;;
          esac
          case "$SCRIPT" in
            *..*) echo "HATA: script yolunda '..' olamaz." >&2; exit 1 ;;
          esac
          if [ ! -f "$SCRIPT" ]; then
            echo "HATA: script bulunamadi: $SCRIPT" >&2
            exit 1
          fi
          # shellcheck disable=SC2086
          python "$SCRIPT" $ARGS

      - name: Kosu ciktilarini artifact yap
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kisisel-pilot-out
          path: out/
          if-no-files-found: ignore
          retention-days: 7
